{
  "buildCommand": "npm run vercel-build",
  "outputDirectory": ".output/public",
  "installCommand": "npm install",
  "framework": "nuxt",
  "env": {
    "NITRO_PRESET": "vercel"
  },
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}